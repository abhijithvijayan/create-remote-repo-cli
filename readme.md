# node-cli-starter

## Getting started

**Click the "Use this template" button.**

Alternatively, create a new directory and then run:

```
$ curl -fsSL https://github.com/abhijithvijayan/node-cli-starter/archive/master.tar.gz | tar -xz --strip-components=1
```
